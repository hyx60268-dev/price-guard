# 2026-09-23 cost and discovery investigation

## Verified before this patch

- Previous code SHA: `394e2fbcd94ddbcd3bf0065e2f3bedb7067fb0d9`.
- Run #856 (`35824959025`) was cancelled while pending, NOT run after lock release.
- Same SHA later ran successfully, including #863 (`35835390089`), job `107102936755`; scan, discovery cache refresh and Pages deployment all succeeded.
- Public status at `2026-09-23T08:50:10.980Z`: 640 inventory items across メロン (96), 桃香雅虎 (49), 老板雅虎 (495); Yahoo 70 live and 570 deferred/cached; Xianyu 30 attempts, 0 newly verified, 2 legacy cached. This was NOT a full live inventory rescan.
- Discovery source scan at `2026-09-23T05:59:34.331Z`: 6 candidates, 0 ready, Mercari 5 / Yahoo 1. Source log: Mercari 26 keyword cards, 19 sellers; Yahoo capped at 80 seeds / 40 sellers / one page.

## Root causes with observed evidence

1. Mercari search renders a `次へ` LINK with an opaque `page_token`; old code only scrolled and searched for a `もっと見る` button. Browser confirmed first page 25 links and next page 20 links with a new next token.
2. Xianyu search cards and recommendations load even when its target detail API is blocked. A real detail page displayed `iframe#baxia-dialog-content` pointing to a CAPTCHA, with only recommendations beneath the skeleton target. Old body-text-only detection missed the cross-origin challenge. Logs contain `detail_variant_mismatch 标题=为你推荐` and zero body match.
3. Old cost verifier could accept 3 listings with 0 known sellers and used search-card price without target-detail price verification. All legacy automatic evidence must be reverified, not preserved as trusted v6 data. Manual inputs remain intact.
4. `Number(null) === 0` produced fictitious zero-cost discovery profit in backend AND UI.
5. Live inventory without hand-authored Chinese queries was searched using untranslated Japanese text. Added centralized query translation; unresolved identity still fails closed.

## Preserved user constraints

- Three managed shops, not just メロン.
- China-limited, sold, > JPY 5,000 (latest preference), same seller / same physical item >= 2 records within 30 days; prefer 2-day then 7-day then 30-day tiers.
- Exclude owned/history/uploaded goods; do not mix colors, characters, card designs, quantities, single/whole-box or bundles.
- Unknown cost is not zero, and cannot establish JPY 1,500 minimum profit.

## Verification and remaining work

- Local 103 automated regression tests passed; these are code tests, NOT 103 products or an inventory audit.
- Mercari next-link navigation was verified in a real browser. Its relative timestamp is now scoped to description siblings and explicitly labelled as a date clue, NOT a proven sale timestamp; such candidates cannot be marked fully ready from cost alone.
- Xianyu remains blocked by a slider in the inspected browser. No CAPTCHA solving, alternate-session bypass, or fabricated cost was performed. Live successful target-detail extraction remains unverified.
- This patch adds code SHA and source/rejection counts to published status, changes discovery cache to 1h, and adds bounded pagination + least-recently-checked seller rotation. Budget or page caps are not full-market coverage.
- After publishing, inspect the NEW commit's actual workflow result AND public status codeSha. A queued run is not publication, and a green publication does not mean Xianyu cost verification succeeded.
