# 闲鱼成本：纯云端约束和验收状态

2026-09-23：用户要求电脑、手机全部关机后仍由云服务执行。不交付依赖本地常驻的采集器；本地方案未合并到主分支。

已核实代码 `6b22932c1a7beadfd313a52f9751047374da1224` 的运行 `35849121819` 完成网页发布，公开状态时间 `2026-09-23T11:06:29.969Z`。三个店铺共 641 件，本轮 Yahoo 实时检查 36 件，其余 605 件延后；这不是 641 件全量比价完成。

本轮闲鱼实际尝试 1 件，详情返回 `detail_login_required`，新增自动成本 0；快速验收 `35845450229` 返回 `detail_blocked`。加载过登录文件、搜索返回卡片、网页发布成功都不能算成本成功。

本次改动在公开状态增加 `cloudCost`，按当前详情证据、独立卖家、价格一致性及有效期计算可用自动参考。主工作流在网页发布之后单独验收闲鱼成本：来源被阻断或没有合格自动参考时明确失败，同时保留已发布的 Yahoo 数据和人工费用。该改动只修正验收，不代表恢复了闲鱼访问。

服务端入口核查：

- 官方 `alibaba.idle.isv.item.query` 需要应用授权、聚石塔内调用，入参为商品 ID；不能仅凭名称认定为可直接使用的全站搜索 API。当前未取得并验证满足这些商品范围的授权入口：https://open.alitrip.com/docs/api.htm?apiId=49488&source=search
- 官方接入说明要求 AppKey、应用密钥及用户授权：https://open.goofish.com/doc/development/dev/server.html
- 已检查的 Apify 搜索产品明确不支持商品详情，因此不作为成本解决方案：https://apify.com/gantianca/xianyu-goofish-search

尚未完成：获得可在现有云环境实际读取目标商品详情、销售数量/款式、实价和独立卖家的数据入口，并验收至少一条真实自动成本。不能承诺更换服务器就能恢复访问，不能将搜索卡片价格或其他平台价格冒充闲鱼核验成本。
